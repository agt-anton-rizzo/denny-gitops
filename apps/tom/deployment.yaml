apiVersion: apps/v1
kind: Deployment
metadata:
  name: tom
  namespace: denny-agents
  labels:
    app.kubernetes.io/name: tom
    app.kubernetes.io/part-of: denny
    denny.bot/archetype: router
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: tom
  template:
    metadata:
      labels:
        app.kubernetes.io/name: tom
        app.kubernetes.io/part-of: denny
        denny.bot/archetype: router
    spec:
      shareProcessNamespace: true
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
        - name: agent
          image: docker.io/library/tom-agent:latest
          imagePullPolicy: Never
          command: ["/opt/denny/entrypoint.sh"]
          stdin: true
          tty: true
          envFrom:
            - configMapRef:
                name: tom-config
            - secretRef:
                name: tom-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 4Gi
          volumeMounts:
            - name: home
              mountPath: /home/tom
            - name: shm
              mountPath: /dev/shm
      volumes:
        - name: home
          persistentVolumeClaim:
            claimName: tom-home
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 2Gi
