apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: michael-home
  namespace: denny-agents
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: michael-home
  resources:
    requests:
      storage: 10Gi
